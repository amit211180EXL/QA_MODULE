# Next.js resolves internal webpack loaders (e.g. next-flight-client-entry-loader) from its
# package root. With pnpm’s isolated node_modules, hoisting `next` avoids "Module not found"
# for those loaders in monorepos (especially on Windows).
public-hoist-pattern[]=next
public-hoist-pattern[]=@next/*
